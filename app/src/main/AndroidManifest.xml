<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".activity.MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Drakor"
        tools:targetApi="31">
        <activity
            android:name=".activity.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name=".activity.drama.DetailActivity" android:label="Detail Activity"/>
        <activity android:name=".activity.home.NavActivity" android:label="Nav Activity"/>
        <activity android:name=".activity.home.HomeActivity" android:label="Home Activity"/>
        <activity android:name=".activity.designpatternmvvm.view.LoginActivity" android:label="Login Activity"/>
        <activity android:name=".activity.designpatternmvvm.view.RegisterActivity" android:label="Register Activity" />
        <activity android:name=".activity.user.UserActivity" android:label="User Activity" />
        <activity android:name=".activity.home.ImageLoaderActivity" />
        <activity android:name=".activity.home.LocationActivity" />
    </application>


<!--    Izin untuk penggunaan internet-->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

<!--    Izin untuk akses lokasi-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

</manifest>